"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isWithinLimit = void 0;
/**
 * Checks whether a value falls inside an inclusive `[min, max]` range, with
 * support for wraparound ranges where `max < min` (e.g. `min: 20, max: 5` on
 * the hours picker means 8 PM through 5 AM next day).
 */
const isWithinLimit = (value, min, max) => max < min ? value >= min || value <= max : value >= min && value <= max;
exports.isWithinLimit = isWithinLimit;
//# sourceMappingURL=isWithinLimit.js.map